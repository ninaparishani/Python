Create a python user interface app that is a basic notepad. It will be a large textbox inside the window. Only basic text will be allowed so no formatting is required. At the top of the form, Will be a menu with save, save as, and copy and paste functions. Create new files or save over existing ones using the text that is in the textbox.